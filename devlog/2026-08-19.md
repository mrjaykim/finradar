## 오늘 한 일
- DART API 일일 호출 한도 실측 확인 (이용현황 페이지 직접 조회: 40,000건, 웹서치로 찾은 10,000건 정보는 부정확했음)
- 병렬 처리(concurrent.futures) 도입 전 소규모(80건)/지속부하(1,000건) 단계적 테스트로 안전한 동시성 수준(5) 검증, 순차 대비 약 3.4배 처리량 개선
- 부실기업(756건)+정상기업(2,904건) 필요 API 호출 수를 로컬 계산으로 사전 산출(총 39,548건), 오늘 한도 초과분(628건)은 다음 실행으로 자동 이월되게 설계
- 배치 재무제표 수집 스크립트(scripts/backfill_financial_statements.py) 작성 - NOT EXISTS 기반 재실행 이어짐, 일일 호출 목표치(30,000, 이후 조정) 도달 시 안전 종료, 020/429 차단 신호 감지 시 즉시 중단하는 이중 안전장치
- 심각한 버그 발견 및 수정: mart.filing upsert 누락으로 실데이터 응답이 전부 FK 위반 실패, 배치 단위 rollback으로 같은 배치의 정상 데이터까지 연쇄 삭제됨 - 오늘 실제 API 호출 11,364건 중 약 10,188건이 낭비됨
- 버그 수정: mart.filing upsert 추가, SAVEPOINT/ROLLBACK TO SAVEPOINT로 항목별 DB 쓰기 격리(한 건 실패가 배치 전체를 되돌리지 않도록)
- 수정 후 재실행: 20,000/20,000건 처리(성공 14,814건, 데이터없음 5,186건), 51분 소요, 실패 0건
- 부실기업 756건 전량 처리 완료, 정상기업은 진행 중 (남은 pending 18,371건은 내일 자동 이어짐)

## 막힌 것
- 배치 처리 4시간 경과 후에도 안 끝나서 확인해보니, mart.filing FK 의존성을 놓친 버그로 실제 데이터를 받고도 저장에 실패, 게다가 배치 단위 rollback이 정상 데이터까지 같이 삭제하는 이중 문제였음
- 근본 원인 진단(FK 누락) → SAVEPOINT로 격리 수정 → 실측 검증(20,000건 무사고)으로 해결
- 오늘 이 버그로 API 호출 약 10,188건이 낭비됨 (다행히 이전 세션 커밋 데이터는 전혀 영향 없음, 낭비분은 재수집으로 복구)

## 내일 할 일 (다음 세션)
- 같은 스크립트 재실행으로 남은 정상기업 재무제표 수집 이어가기 (NOT EXISTS 자동 이어짐, 남은 pending 약 18,371건)
- 전체 수집 완료 후 최종 검증 및 ADR 정리 (배치 처리 안전장치 설계, FK 버그 사례)
