## 오늘 한 일
- PostgreSQL 17 설치, postgres 계정 비밀번호 재설정 (trust 모드 임시 전환 → ALTER USER → 원복 → 검증)
- Gemini/ChatGPT와 스키마 설계안을 여러 차례 교차 검토
- 최종 스키마 확정: raw.dart_financial_statement(대리키), mart.company, mart.filing, mart.financial_statement(CHECK 제약 + 복합 UNIQUE + 인덱스)
- finradar DB 생성, schema.sql 실행하여 스키마 2개/테이블 4개/인덱스 3개 전부 적용 완료
- .env에 DB 접속 정보(DB_HOST, DB_PORT, DB_NAME, DB_USER) 추가

## 막힌 것
- winget 설치 PostgreSQL의 postgres 계정 비밀번호를 몰라 pg_hba.conf trust 모드 전환으로 재설정
- account_id 등 DART API 특유의 결측/비표준 값 이슈를 여러 차례 검토 → raw는 대리키로 무조건 수용, mart는 CHECK+UNIQUE로 방어하는 구조로 결론

## 내일 할 일
- get_financial_statements.py를 확장해서 삼성전자 데이터를 실제 mart 테이블에 저장
- docs/etl_notes.md에 account_id 결측치 처리 원칙 기록
