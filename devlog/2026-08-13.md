## 오늘 한 일
- 재무제표 수집 범위 논의 중 DART API가 2015년 이전 데이터를 제공하지 않는다는 공식 제약 발견
- 756건(2010년 이후 실질부실) 중 318건(42%)이 이 제약으로 재무 피처를 전혀 확보할 수 없음을 실측 확인
- 수집 범위 확정: 부실기업은 상장폐지 시점 기준 과거 최대 12분기(2015-01-01 하한), 정상기업도 12분기를 positive 사건 연도 분포에 맞춰 층화 예정
- industry_code(업종코드) 확보 여부 판단: DART company.json API로 원시 KSIC 코드는 구할 수 있음을 확인 (ADR-009/010의 "구할 수 없다"는 전제가 부정확했음을 정정)
- 정상기업 범위를 ADR-010의 목표치(2,268)가 아닌 is_active=true 전체(2,904)로 확대해 재작업 방지
- scripts/backfill_company_industry_code.py 작성 및 실행: 3,660건(부실 756+활성 2,904) 전부 성공, 실패 0건
- ADR-010에 수집 범위 결정과 업종 전제 정정을 반영

## 막힌 것
- 백필 스크립트 실행 시간이 예상(20~60분)보다 훨씬 오래 걸림 (실제 약 2시간) - 딜레이(0.3~1.0초)만으로 소요시간을 추정했는데, 실제 API 왕복 응답시간이 예상보다 커서 분당 처리량이 낮았음. 다음에 대량 배치 API 호출 시간을 추정할 때는 소규모 샘플로 실측 후 전체 시간을 추정하는 방식이 필요
- 로그 파일이 stdout 버퍼링으로 비어 보여 순간적으로 "멈춘 것 아닌가" 혼동 - DB를 직접 조회해서 최근 갱신 시각 확인으로 정상 진행 중임을 검증

## 내일 할 일 (다음 세션)
- 실제 배치 재무제표 수집 스크립트 작성 (부실 756건 + 정상 2,904건 대상, 각 최대 12분기)
- 정상기업 최종 샘플링 로직(시장 비율 층화, positive 연도 분포 고려)은 4주차로 계속 보류하되, 수집 자체는 전체 대상으로 진행
