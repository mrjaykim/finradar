## 오늘 한 일
- get_financial_statements.py를 확장해 DB 저장 로직 구현 (raw → company → filing → financial_statement 순서)
- 실제 삼성전자 데이터로 1차 실행 성공 (229건), 재실행으로 멱등성 검증 (raw는 append, mart는 upsert 유지)

## 막힌 것
- 실제 DART 데이터(SCE 자본변동표)에서 account_id+ord가 같아도 account_detail(자본 구성요소)이 달라 기존 UNIQUE 제약과 충돌하는 문제 발견 (전체 229건 중 105건, 46%)
- account_detail 컬럼 추가 + UNIQUE 제약 확장으로 해결, 라이브 DB와 schema.sql 동기화, ADR-004로 근거 기록

## 내일 할 일
- (2주차 계획: 수집 대상 기업 리스트 확정, 배치 스크립트로 여러 기업 일괄 수집)
